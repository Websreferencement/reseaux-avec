{% extends 'App::Page/home.html.twig' %}

{% block page %}

	<div class="flashes text-center">
		<a href="{{path('frontend', {'uri': 'actualites'})}}" class="btn btn-small">
			Revenir à la liste des actualités
		</a>
	</div>

	<div class="page-header">

		<h1>{{news.title}} <small> - paru le {{news.createdAt}}</small></h1>

	</div>

	{{news.content|raw}}

{% endblock %}